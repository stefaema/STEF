[project]
name = "stef-shared"
version = "0.1.0"
description = "What more than one module needs: the wire contract, and where things live"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = [".."]
testpaths = ["tests"]
cache_dir = "build/pytest"
