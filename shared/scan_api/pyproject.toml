[project]
name = "stef-scan-api"
version = "0.1.0"
description = "What the scan subsystem serves, as both ends see it"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["../.."]
testpaths = ["tests"]
cache_dir = "build/pytest"
