[project]
name = "stef-fw-api"
version = "0.1.0"
description = "The wire contract both ends compile, as C and as ctypes"
requires-python = ">=3.11"

[tool.stef]
generated = "python tools/abi_gen.py --check"

[tool.pytest.ini_options]
pythonpath = ["../.."]
testpaths = ["tests"]
cache_dir = "build/pytest"
