[project]
name = "stef-subsystem"
version = "0.1.0"
description = "What Transport, Capture and Detection have in common"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["../.."]
testpaths = ["tests"]
cache_dir = "build/pytest"
