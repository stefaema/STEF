[project]
name = "stef-paths"
version = "0.1.0"
description = "Where a module's shipped files are, and where this machine's own files go"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["../.."]
testpaths = ["tests"]
cache_dir = "build/pytest"
