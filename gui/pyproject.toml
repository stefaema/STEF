[project]
name = "stef-gui"
version = "0.1.0"
description = "Operator GUI, a client of the orchestrator"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = [".."]
testpaths = ["tests"]
cache_dir = "build/pytest"
python_files = ["test_*.py"]
