[project]
name = "stef-transport"
version = "0.1.0"
description = "PC-side client of the film transport"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = [".."]
testpaths = ["tests"]
cache_dir = "build/pytest"
