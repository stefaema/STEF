#!/usr/bin/env bash
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"

refs=()
while read -r _local_ref _local_sha remote_ref _remote_sha; do
    refs+=("$remote_ref")
done

exec "$ROOT/ci_cd/run.py" stage-for ${refs[@]+"${refs[@]}"}
